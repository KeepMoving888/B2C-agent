# 知识库数据

此目录包含多语言客服知识库的真实业务数据（FAQ、商品信息、订单模板等）。

真实业务数据不包含在开源版本中。部署时请替换为自有知识库数据。

## 数据格式说明

知识库采用 JSONL 格式，每条记录包含：
- `id`: 唯一标识
- `category`: 分类（FAQ/商品/订单/售后等）
- `content`: 内容文本
- `lang`: 语言代码
- `embedding`: 向量（运行时生成）
