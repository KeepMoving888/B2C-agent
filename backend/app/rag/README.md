# RAG 检索增强模块

此模块包含混合检索（向量 + BM25 + RRF 融合）+ Cross-Encoder 重排序 + 反幻觉校验的完整实现。

该模块为项目核心算法，仅展示架构设计，具体实现不包含在开源版本中。

## 架构说明

- **混合检索**：BGE-M3 向量检索 + BM25 关键词检索 + RRF 融合排序
- **多语言支持**：高频语言分区路由，提升跨语言召回率
- **反幻觉防护**：引用溯源 + 置信度阈值 + 答案一致性校验
- **质量评估**：Recall@K / MRR / NDCG / Precision 学术指标
